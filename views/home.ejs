<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD (Create, Read, Update & Delete)</title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/js/utility.js"></script>
</head>

<body>
    <div>
        <h1>JSON CRUD ( Create, Read, Update & Delete )</h1>
        <a href="/add" class="create">Create</a>
        <table>
            <thead>
                <tr>
                    <th>No</th>
                    <th>Name</th>
                    <th>Height</th>
                    <th>Weight</th>
                    <th>Birth Date</th>
                    <th>Is Married</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach((value, index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>
                        <td>
                            <%= value.name %>
                        </td>
                        <td>
                            <%= value.height %>
                        </td>
                        <td>
                            <%= value.weight %>
                        </td>
                        <td>
                            <%= value.birthdate %>
                        </td>
                        <td>
                            <%= value.married ? 'Yes' : 'Not Yet' %>
                        </td>
                        <td>
                            <a href="/edit/<%= index %>" class="update-button">Update</a>
                            <a href="/delete/<%= index %>" onclick="return confirmation('<%= value.name %>')"
                                class="delete-button">Delete</a>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>